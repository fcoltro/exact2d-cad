[package]
# Proprietary, all rights reserved â€” never publish to crates.io.
publish = false
name = "exact2d_geometry"
version = "0.5.0"
edition = "2021"

[dependencies]
exact2d_algebra = { path = "../exact2d_algebra" }
exact2d_integer  = { path = "../exact2d_integer" }
