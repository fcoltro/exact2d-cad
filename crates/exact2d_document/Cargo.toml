[package]
# Proprietary, all rights reserved — never publish to crates.io.
publish = false
name = "exact2d_document"
version = "0.3.2"
edition = "2021"

[dependencies]
exact2d_algebra  = { path = "../exact2d_algebra" }
exact2d_geometry = { path = "../exact2d_geometry" }
